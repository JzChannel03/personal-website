import ThemeMode from "@/components/ThemeMode";

export default async function Main() {
  return (
    <section className="relative bg-white full">
      Hello
      <ThemeMode />
    </section>
  );
}
